import { Box, Stack } from '@mui/material';
import { useSelector } from 'react-redux';

const HeroesPage = () => {
  return (
    <Stack>
      <Box>
        <p>Heroes Page</p>
      </Box>
    </Stack>
  );
};

export default HeroesPage;
