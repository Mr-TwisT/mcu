import { createTheme } from '@mui/material';

const marvelTheme = createTheme({
  palette: {
    primary: {
      main: '#a31b1f',
      // dark: '',
      // light: '',
    },
    secondary: {
      main: '#701316',
      // dark: '',
      // light: '',
    },
    complementary: {
      main: '#1ba39e',
      // dark: '',
      // light: '',
    },
    // black: '#000',
    // white: '#fff',
  },
});

export default marvelTheme;
